# 2026-07-23 Portfolio README self-hosted server update

> 2026-07-24更新: 実運用URLを掲載しない判断は、共有デモサーバーでQR体験を提供する現行方針に置き換えられた。接続URLは公開するが、管理資格情報と保存データは引き続き公開しない。

- 対象ステップ: QR登録とセルフホスト型スコアサーバーの実運用をポートフォリオREADMEへ反映。
- 変更ファイル: `README.md`、本記録。
- 採用した判断: 自宅でセルフホストしているPythonサーバーを実運用構成として明記し、QR登録、スマートフォンとの状態同期、スコア保存、ランキング配信の責務を独立した節で説明する。
- 理由・根拠: 展示機だけでなく、参加者の登録から結果保存までを含むシステムを実会場で運用した点はポートフォリオ上の成果であり、単なる任意サンプルサーバーと記載すると実績が伝わらないため。
- 採用した判断: スマートフォンはプレイヤー情報と操作状態だけを送り、スコアは展示機側で計算した確定結果だけをサーバーへ送る責務分離を記載する。
- 理由・根拠: スマートフォンから任意スコアを送信できない設計と、サーバーをランキングの正本にした設計意図を短い説明で示すため。
- 採用した判断: 実運用サーバーのURL、実ユーザー情報、個別スコア、セッション履歴はREADMEと公開データへ掲載しない。
- 理由・根拠: サーバー機能と運用実績は公開価値がある一方、接続先や参加者データはポートフォリオ閲覧に不要であり、セキュリティとプライバシーを優先するため。
- 確認結果: README相対リンク2件、`git diff --check`、`npm run typecheck`、`npm run lint`、`npm test`（281件PASS）、`npm run build`がすべてPASS。
